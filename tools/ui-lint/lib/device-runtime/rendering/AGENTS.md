<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-09-21 | Updated: 2026-09-21 -->
# rendering

## Purpose

Per-device rendering runtimes derived from a descriptor.

## Key Files

| File | Description |
|---|---|
| `viewport-runtime.mjs` | `buildViewportRuntime` |
| `dpr-runtime.mjs` | `buildDprRuntime` |
| `safe-area-runtime.mjs` | `buildSafeAreaRuntime` |
| `visual-viewport.mjs` | `buildVisualViewportRuntime` |

## For AI Agents

### Working In This Directory

- No special constraints beyond the parent directory guidance.

### Testing Requirements

- Covered by `tests/config/device-runtime.spec.js`. Run `cd tools/ui-lint && npx playwright test <spec>` (no playwright.config file; defaults apply).

### Common Patterns

- No special constraints beyond the parent directory guidance.

## Dependencies

### Internal

- No special constraints beyond the parent directory guidance.

### External

- No special constraints beyond the parent directory guidance.

<!-- MANUAL: Any manually added notes below this line are preserved on regeneration -->

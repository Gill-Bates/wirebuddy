# WireBuddy Configuration
# Copy this file to settings.env and adjust values

# --- App ---
WIREBUDDY_SECRET_KEY="" # Generate with: head -c 32 /dev/urandom | base64
WIREBUDDY_DATA_DIR=/data
LOG_LEVEL=INFO
